using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class AIBehaviors
{
    public static INode.State MoveTo(Transform from, Vector3 to)
    {
        // 간단한 이동 로직이라던가..
        return INode.State.FAILED;
    }
}
